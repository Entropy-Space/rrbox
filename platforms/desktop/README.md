# ResearchBox for desktop

This directory is reserved for the macOS, Linux, and Windows composition layer.
It will connect approved native folders through the shared `WorkspaceBackend`
and `Workspace` capabilities without introducing desktop APIs into reusable
packages.
