# ResearchBox for desktop

This directory is reserved for the macOS, Linux, and Windows composition layer.
It will connect approved native folders to the shared `VirtualFileSystem`
contract without introducing desktop APIs into reusable packages.
