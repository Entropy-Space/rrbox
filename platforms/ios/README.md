# ResearchBox for iOS

This directory is reserved for the iOS composition layer. It will provide an
iOS application-storage implementation of the shared `WorkspaceBackend` and
`Workspace` capabilities without changing the viewer/core protocol.
