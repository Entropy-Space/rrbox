# ResearchBox for iOS

This directory is reserved for the iOS composition layer. It will provide an
iOS application-storage implementation of the shared `VirtualFileSystem`
contract without changing the viewer/core protocol.
